name: ld-1000g-region-compute
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.10
  - pysam>=0.22
  - pandas>=2.0
  - requests>=2.28
  - htslib>=1.18    # tabix
  - plink=1.9        # subprocess for r² compute
