interface:
  display_name: "Rust Symbol Analyzer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $rust-symbol-analyzer for this task."

policy:
  allow_implicit_invocation: false
