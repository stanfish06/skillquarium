interface:
  display_name: "M07 Concurrency"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $m07-concurrency for this task."

policy:
  allow_implicit_invocation: false
