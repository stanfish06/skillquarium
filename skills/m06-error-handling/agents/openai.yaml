interface:
  display_name: "M06 Error Handling"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $m06-error-handling for this task."

policy:
  allow_implicit_invocation: false
