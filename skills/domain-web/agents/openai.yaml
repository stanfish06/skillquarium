interface:
  display_name: "Domain Web"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $domain-web for this task."

policy:
  allow_implicit_invocation: false
