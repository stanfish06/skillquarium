interface:
  display_name: "Rust Router"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $rust-router for this task."

policy:
  allow_implicit_invocation: false
