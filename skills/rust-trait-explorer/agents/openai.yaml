interface:
  display_name: "Rust Trait Explorer"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $rust-trait-explorer for this task."

policy:
  allow_implicit_invocation: false
