interface:
  display_name: "Domain Embedded"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $domain-embedded for this task."

policy:
  allow_implicit_invocation: false
