interface:
  display_name: "Domain Cloud Native"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $domain-cloud-native for this task."

policy:
  allow_implicit_invocation: false
