interface:
  display_name: "Rust Call Graph"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $rust-call-graph for this task."

policy:
  allow_implicit_invocation: false
