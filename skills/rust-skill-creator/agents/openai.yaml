interface:
  display_name: "Rust Skill Creator"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $rust-skill-creator for this task."

policy:
  allow_implicit_invocation: false
