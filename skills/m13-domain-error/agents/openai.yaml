interface:
  display_name: "M13 Domain Error"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $m13-domain-error for this task."

policy:
  allow_implicit_invocation: false
