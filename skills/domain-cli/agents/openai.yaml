interface:
  display_name: "Domain Cli"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $domain-cli for this task."

policy:
  allow_implicit_invocation: false
