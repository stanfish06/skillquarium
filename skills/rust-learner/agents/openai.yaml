interface:
  display_name: "Rust Learner"
  short_description: "Invoke this skill explicitly when needed."
  default_prompt: "Use $rust-learner for this task."

policy:
  allow_implicit_invocation: false
